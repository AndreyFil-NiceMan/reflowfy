apiVersion: v2
name: reflowfy
description: Reflowfy - Horizontally scalable data movement and transformation framework
type: application
version: "0.37"
appVersion: "1.0.11"

dependencies:
  # PostgreSQL - Optional, only deployed if postgresql.enabled=true
  - name: postgresql
    version: "18.2.3"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled

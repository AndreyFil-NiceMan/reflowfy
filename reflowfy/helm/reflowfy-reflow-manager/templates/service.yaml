apiVersion: v1
kind: Service
metadata:
  name: {{ include "reflowfy-reflow-manager.fullname" . }}
  labels:
    {{- include "reflowfy-reflow-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reflowfy-reflow-manager.selectorLabels" . | nindent 4 }}
